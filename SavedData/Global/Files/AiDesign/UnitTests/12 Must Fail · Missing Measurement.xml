<?xml version="1.0" encoding="utf-8"?><!--
  N02 Missing Measurement

  MUST BE REFUSED. A number that never says what it measures, and so never says
  how its observations merge either. Two errors on one row, naming the class and
  the variable — the no-fail rule in its plainest form.

  A UNIT TEST DESIGN THAT IS SUPPOSED TO FAIL. Bundled and read-only:
  suite [25] asserts the check CATCHES this, so a fix here would quietly
  remove the test.
--><gorp name="N02 Missing Measurement"><!-- nos-uml-state {"mode":"ai-design","title":"N02 Missing Measurement","layout":{"ut.unmeasured.Input":{"x":60,"y":60,"w":280,"h":140},"ut.unmeasured.Use":{"x":420,"y":60,"w":280,"h":140},"ut.unmeasured.Output":{"x":780,"y":60,"w":280,"h":140}}} --><comment view="class">MUST BE REFUSED. Input.amount is a double that never says what it measures, and therefore never says how repeat observations merge either.

Two errors on one row, each naming the class and the variable — the no-fail rule in its plainest form.</comment><expect>Check must report Input.amount twice: no measurement, and no collapse policy. Run must be greyed out while either stands.

A green check here means the no-fail rule stopped being enforced.</expect><model namespace="ut.unmeasured.Input"><variables><variable name="amount" type="double" /></variables></model><algorithm namespace="ut.unmeasured.Use"><input model="ut.unmeasured.Input" /><output model="ut.unmeasured.Output" /><operations><operation name="use" returns="double" /></operations></algorithm><model namespace="ut.unmeasured.Output"><variables><variable name="result" type="double" collapse="Monotone"><unit basis="\one" namespace="D-SI" /></variable></variables></model></gorp>